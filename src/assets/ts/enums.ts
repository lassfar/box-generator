
export enum EButtonType {
  button = "button",
  submit = "submit",
  reset = "reset",
}

export enum EButtonVariant {
  primary = 'bg-primary text-white',
  secondary = 'bg-black text-white',
}

export enum EInputType {
  text = "text",
  email = "email",
  tel = "tel",
  date = "date",
  number = "number",
  password = "password",
}

export enum EInputVariant {
  primary = "border-2 border-solid border-gray-300 rounded-lg focus:outline-none focus:border-indigo-400 py-1.5 px-3"
}
